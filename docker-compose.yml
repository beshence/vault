services:
  vault:
    build: .
    image: vault:latest
    ports:
      - "${PORT:-8080}:8080"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:8080/api/v1/ping || exit 1"]
      interval: 1m
      timeout: 5s
      retries: 5

